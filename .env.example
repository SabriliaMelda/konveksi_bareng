# Kosongkan untuk development (otomatis per platform)
# Isi URL production saat deploy, misal: API_URL=https://api.domain.com
API_URL=

# Set TRUE to skip login and go straight to HomeScreen (dev only)
DEV_AUTH_BYPASS=FALSE
